version: '3.8'

services:
  portfolio:
    build: .
    container_name: matheus-portfolio
    restart: unless-stopped
    ports:
      - "127.0.0.1:8086:80"
    environment:
      - NODE_ENV=production
